{
  "name": "seqcj",
  "version": "0.1.0",
  "description": "CoinJoin coordinator for Sequentia: Chaumian blind-signature credentials over Confidential Transactions, with a parent-chain BTC path through the SBTC peg.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node coordinator.mjs",
    "check": "node --check coordinator.mjs && node --check client.mjs && node --check blindsig.mjs",
    "test": "node --test 'test/**/*.test.mjs'"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
